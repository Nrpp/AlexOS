# AlexOS Mobile

Placeholder for the future Android/iPad client. Not started in the
Foundation milestone. When it begins, it shares `packages/*` (types,
core client, hooks, design tokens) with `apps/web` rather than
duplicating any of it - the same reason AlexOS is a monorepo.
